#include "function_pointers.h"
/**
 * op_add - adds two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the addition value of the two arguments
*/

int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - subtract two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the submission value of the two arguments
*/

int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - multiplies two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the multiplication value of the two arguments
*/

int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_add - divides two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the division value of the two arguments
*/

int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_add - the remainder of the two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the addition value of the two argument sbdamn
*/

int op_mod(int a, int b)
{
	return (a % b);
}
