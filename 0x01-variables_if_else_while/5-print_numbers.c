#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Return Always 0 (success)
 */

int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
		putchar(digit);
	}
	putchar('\n');
	return (0);
}

