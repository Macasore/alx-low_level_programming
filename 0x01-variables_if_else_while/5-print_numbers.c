#include <stdio.h>
/**
* main - prints all single digit numbers of base10 starting from 0
*
* Return: Always 0.
*/
int main(void)
{
	int i;

	while (i != 10)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");
	return (0);
}
