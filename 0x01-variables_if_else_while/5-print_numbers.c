#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Return Always 0 (success)
 */

int main(void)
{
	char digit;

	for (digit = '0'; digit < '10'; digit++)
	{
		printf(digit);
	}
	printf('\n');
	return (0);
}

